@model IEnumerable<Khaira_Freight.Models.table_driver>

@{
    ViewBag.Title = "Profile";
    Layout = "~/Views/Shared/DriverLayout.cshtml";
}

<style>
    #main {
        //background-image: url('/Content/images/profile.jpg.jpg');
        background-attachment: fixed;
        background-size: 50% 150%;
    }

    /*#container {
        align-content: center;
        border: solid;
        border-color: lightcoral;
        width: 50%;
        margin-left: 100px;
    }*/
    #container {
        background-color: lavender;
        padding: 30px;
        margin: 50px auto;
        box-shadow: 1px 1px 25px rgba(0, 0, 0, 0.35);
        border-radius: 10px;
        border: 6px solid #305A72;
        max-width: 600px;
          min-width:min-content;
    }

    h4 {
        padding: 10px;
    }

    h1 {
        margin-left: 150px;
    }

    #btn_style {
        color: #494949 !important;
        text-transform: uppercase;
        text-decoration: none;
        background: #ffffff;
        padding: 20px;
        border: 4px solid #494949 !important;
        display: inline-block;
        transition: all 0.4s ease 0s;
    }

        #btn_style:hover {
            color: #ffffff !important;
            background: #f6b93b;
            border-color: #f6b93b !important;
            transition: all 0.4s ease 0s;
        }
</style>
<div id="main">


    <div id="container">

        <h3 id="btn_style">
            PROFILE
        </h3>

        <h4>
            @Html.DisplayNameFor(model => model.first_name):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.first_name)
            }
        </h4>

        <h4>
            @Html.DisplayNameFor(model => model.last_name):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.last_name)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.address):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.address)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.postal_code):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.postal_code)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.city):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.city)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.province):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.province)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.corporation):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.corporation)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.phone):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.phone)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.dob):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.dob)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.nationality):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.nationality)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.gender):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.gender)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.pp_number):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.pp_number)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.license_number):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.license_number)
            }

        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.license_expiry):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.license_expiry)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.medical_date):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.medical_date)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.rate):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.medical_date)
            }
        </h4>


        <h4>
            @Html.DisplayNameFor(model => model.rate_team):
            @foreach (var item in Model)
            {

                @Html.DisplayFor(modelItem => item.rate_team)
            }
        </h4>

        @foreach (var item in Model)
        {

            <h4 id="btn_style"> @Html.ActionLink("Schedule", "Calender", new { id = item.driver_id }) </h4>

            @*<h4 id="btn_style">@Html.ActionLink("Login Info", "LoginInfo")</h4>*@
            <h4 id="btn_style"> @Html.ActionLink("Edit Profile", "EditProfile", new { id = item.driver_id }) </h4>


        }




    </div>
    @*@Html.ActionLink("Home", "Home", new { id = item.driver_id })*@


</div>

